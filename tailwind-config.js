// tailwind-config.js
tailwind.config = {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Inter', 'Helvetica Neue', 'sans-serif'],
                display: ['Impact', 'Arial Black', 'sans-serif'],
            },
            colors: {
                onyx: '#0a0a0a',
                signature: '#d4af37', // The gold accent for the stars
            }
        }
    }
}
